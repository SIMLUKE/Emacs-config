name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: find -name "#*#" -delete -o -name '*~' -delete -o -name "coding-style-reports.log" -delete -o -name "vgcore.*" -delete
      - run: tests/coding-style/coding-style.sh

